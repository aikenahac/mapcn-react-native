// Generated prop-table docs (plan §5.3: apps/docs/content/generated/props/<name>.mdx).
//
// Deferred to Phase 11 on purpose: apps/docs is still the pre-Fumadocs
// site today, so there is no `content/` directory or MDX pipeline for this
// to write into yet. Building the generator now would mean designing its
// output format twice. Not part of the CI pipeline (ci.yml only runs
// registry:check), so this is a safe no-op until Phase 11 lands.
console.log("[registry:docs] deferred to Phase 11 (docs site rebuild) -- see plan §10, §5.3");
